reconfigure:
  derived_components:
    - dns
  description: Update server details on NSXManager
  allowed_values:
    - true
  format: boolean
  method: UpdateComponent
  params:
    - name
  preprocess: PreProcessHashRefTypeAPI
  sample_value: true
  type: action