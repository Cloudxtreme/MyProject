---
cpu:
  description: Parameter to define the cpu parameters for RP where we can set shares (low/high/normal), limit (0-25K MHz), reservation (0-25K MHz)
  format:
    cpu:
      shares: string
      limit: MHz
      reservation: MHz
  type: parameter
  derived_component:
    - resourcepool
  sample_value:
    cpu:
      share: low
      limit: 10K MHz
      reservation: 10K MHz
memory:
  description: Parameter to define the memory parameters for RP where we can set shares (low/high/normal), limit (0-25K MHz), reservation (0-25K MHz)
  format:
    memory:
      shares: string
      limit: MHz
      reservation: MHz
  type: parameter
  derived_component:
    - resourcepool
  sample_value:
    memory:
      share: low
      limit: 10K MHz
      reservation: 10K MHz
